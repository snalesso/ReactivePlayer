<UserControl
    x:Class="ReactivePlayer.UI.WPF.Views.EditTrackAlbumAssociationView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:Views="clr-namespace:ReactivePlayer.UI.WPF.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Background="White"
    mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" SharedSizeGroup="{x:Static Views:ViewsConstants.TagNamesColumn}" />
            <ColumnDefinition Width="8" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="8" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="8" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock
            Grid.Row="0"
            Grid.Column="0"
            VerticalAlignment="Center"
            Text="Album" />
        <Grid
            Grid.Row="0"
            Grid.Column="2"
            VerticalAlignment="Center">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="8" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Button
                Grid.Column="0"
                VerticalAlignment="Center"
                Content="New" />
            <ComboBox
                Grid.Column="2"
                MinWidth="150"
                VerticalAlignment="Center">
                <ComboBoxItem Content="Album 1" />
                <ComboBoxItem Content="Album 2" />
                <ComboBoxItem Content="Album 3" />
                <ComboBoxItem Content="Album 4" />
            </ComboBox>
        </Grid>

        <TextBlock
            Grid.Row="2"
            Grid.Column="0"
            Text="Track #" />
        <TextBox
            Grid.Row="2"
            Grid.Column="2"
            MinWidth="150" />

        <TextBlock
            Grid.Row="4"
            Grid.Column="0"
            Text="Disc #" />
        <TextBox
            Grid.Row="4"
            Grid.Column="2"
            MinWidth="150" />

    </Grid>
</UserControl>