<UserControl x:Class="ReactivePlayer.App.Desktop.UI.Views.TracksView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:ReactivePlayer.App.Desktop.UI.Views"
             xmlns:ViewModels="clr-namespace:ReactivePlayer.App.Desktop.ViewModels;assembly=ReactivePlayer.App.Desktop"
             mc:Ignorable="d"
             d:DataContext="{Binding Source={d:DesignInstance Type=ViewModels:TracksViewModel, IsDesignTimeCreatable=False}}"
             d:DesignHeight="300"
             d:DesignWidth="300">

    <DataGrid ItemsSource="{Binding TrackViewModels}"
              AutoGenerateColumns="True">

    </DataGrid>

</UserControl>